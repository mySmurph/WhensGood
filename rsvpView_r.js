let domUserInputContainer = document.getElementById("userInputContainer");
let domPasswordText = document.getElementById("passwordText");
let domInputPassword = document.getElementById("passwordInput");
let domSlider = document.getElementById("slider");

domSlider.addEventListener("change", changeSize, false);